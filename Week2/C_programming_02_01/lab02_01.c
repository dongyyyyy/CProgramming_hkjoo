#include <stdio.h>
//1번문제
int main()
{
	int point1 = 95, point2 = 84;
	int total = point1 + point2;

	printf("두 수의  합(total) : %d\n", total);
	printf("두 수의 평균(정수) : %d", total / 2);
	printf("두 수의 평균(실수) : %f", total / 2.0);

	return 0;
}
